extends layout.pug
include utils.pug
block content
  h3 Are you sure you want to delete this review?
  div
    form(action=`/plants/reviews/delete/${review.id}` method='post')
      input(type='hidden' name='_csrf' value=csrfToken)
      button(type='submit') Delete review
      a(href=`/plants/${review.plantId}` role='button') Cancel
